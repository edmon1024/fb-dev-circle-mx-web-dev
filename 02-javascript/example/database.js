import { Book } from './Book.js';

const a = new Book('titulo a','autor a',100,'descripcion a')
const b = new Book('titulo b','autor b',110,'descripcion b')
const c = new Book('titulo c','autor c',130,'descripcion c')

const bookList = [a,b,c]

export { bookList };

